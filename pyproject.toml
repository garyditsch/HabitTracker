[project]
name = "health-tracker"
version = "1.0.0"
description = "Minimalist personal health tracker with public dashboard"
requires-python = ">=3.10"
dependencies = [
    "flask==3.0.0",
    "python-dotenv==1.0.0",
    "werkzeug==3.0.1",
    "gunicorn==21.2.0",
]
